var stage = null;

var init = function(stageElement) {
  stage = stageElement;
};

var load = function(item) {
};

var show = function(item) {
};

var hide = function(item) {
};

module.exports = {
  init: init,
  load: load,
  show: show,
  hide: hide
};
